[package]
name = "api-workbench"
version = "0.1.0"
description = "A fast, keyboard-driven HTTP client"
authors = ["API Workbench"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "api_workbench_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
# Requests are made from Rust, which is what frees the desktop build from CORS
# and lets it reach localhost and the LAN like any other native client.
tauri-plugin-http = "2"

[features]
# Used by `tauri dev`; keeps the devtools out of release bundles.
custom-protocol = ["tauri/custom-protocol"]
